---
description: ANI 产品设计规范 2.0 已冻结；Console 实现须只读遵循并按 SCB 批次改造
globs:
  - frontends/console/**
  - openapi/v1.yaml
alwaysApply: true
---

# 设计规范 2.0 冻结（强制）

## 冻结令

产品设计规范 **2.0 已于 2026-06-25 冻结**。真来源：[DESIGN-SPEC-FREEZE.md](../../DESIGN-SPEC-FREEZE.md)

**禁止**修改下列文档正文：`UI规范-2.0.md`、`产品设计规范-*-2.0.md`（不含已废止的 TDesign 版）。

## 实现前必读（只读）

1. `DESIGN-SPEC-FREEZE.md`
2. `UI规范-2.0.md`
3. `产品设计规范-设计原则-2.0.md` → `Arco组件与Token-2.0` → `页面模板-2.0` → `样式与Tailwind边界-2.0`
4. `frontends/console/docs/CONSOLE-SPEC-COMPLIANCE-BATCHES.md`（当前 SCB 批次）
5. `frontends/console/CONVENTIONS.md`

Console 专项入口：`frontends/console/CLAUDE.md`

## 实现规则

- **组件**：`@arco-design/web-react` 唯一；图标 `@arco-design/web-react/icon`
- **颜色**：Arco Token（`var(--color-*)`）；禁止 Tailwind 色板类替代语义色
- **布局**：Tailwind utilities（仅布局）或 Arco Layout/Space；见样式边界文档
- **页面**：`src/routes/` 路由与页面同文件；列表/表单须 loading / empty / error 三态
- **批次**：存量页按 **SCB-01 → SCB-12** 顺序改造，不得跳批；进度写 `SPRINT-SCB-*.md`，不改冻结规范

## 禁止

- 编辑冻结的设计规范 `.md`
- 全库私自重风格或跳过当前 SCB 批次
- TDesign / Ant Design / shadcn / 平行 `components/ui`

## 验收

每批次：`cd frontends/console && npm run verify`
