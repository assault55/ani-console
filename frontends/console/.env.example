# 本地开发（可选）：`npm run dev` 时把浏览器 /api/v1 请求代理到后端
# 不设置则默认 Core Mock Server；生产构建不经过此代理，由部署侧 Gateway 转发
VITE_API_PROXY_TARGET=http://10.10.1.66:30080

# OIDC 回调地址（与认证服务登记一致）
VITE_OIDC_REDIRECT_URI=http://localhost:5173/login/callback
